language:
  php

php:
  - 5.3
  - 5.4

before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar update -v

script:
  - ./Vendors/bin/phpunit --colors --coverage-text -c Conf/phpunit.xml
